今日完成任务：
一、AC对接成功
接口已接，圆满实现了爬取部分与后端的对接，剩余问题详见sandbox\ACwork_pros.md；

A&C联调结果：
爬虫日志无ERROR    
数据存入数据库    
图片上传MinIO    
任务状态正确上报    
轮询等待新种子    
联调成功！

二、AC对接后续优化实现
①优雅退出与信号处理优化（后端可选调用）
预期效果：爬虫进程收到 SIGINT（Ctrl+C）或 SIGTERM 信号后，不再拉取新种子，等待当前正在处理的页面完成抓取和图片上传，最后输出统计信息（总处理数、成功数、失败数、图片下载数）并正常退出。
②图片上传并发数可配置
调整说明：不再使用环境变量，改为从后端配置的 concurrency 字段读取。
③配置字段命名统一
调整说明：后端已新增 default_allowed_domains 字段，爬虫直接使用该字段，不再做任何字段映射。

该任务已完成并上传至main，详情见sandbox\A2.0.md；

三、（下午）爬取华科教师网页
爬取华科教师网页http://faculty.hust.edu.cn/
全量爬取3000 个 HTML 文件，最终将 sandbox/data/ 目录打包为 hust_faculty_data.tar.gz，并提供给成员 B


明日优化任务：
1.爬取Redis并行启用
2.支持断点续爬（应对断网等特殊中断情况）
3.爬取速率优化：实现自适应延迟、增加 缓存 DNS、使用 更快的 JSON 解析器