# A 模块 V2.0 接口适配报告

**日期**：2026-06-15
**成员**：成员 A
**任务**：基于 C 第二版接口文档完成 P0 适配，对齐统一响应格式、新增字段、健康检查

---

## 适配背景

C 于 2026-06-15 提供第二版接口文档，引入三项变更：所有接口统一响应信封 `{code, msg, data}`；`pagesnapshot` 和 `crawl/start` 新增 `task_type/user_prompt` 等字段；`task_type` 取值统一为 `preview | formal`。A 模块完成对比审查后向 C 提出 6 个待确认问题，C 逐一回复后 A 实施 P0 适配。

---

## 修改文件清单

### sandbox/standalone_crawler/api_client.py
- `_request`：增加 `{code, msg, data}` 信封解包；`code != 200` 且非 5xx 抛 `APIClientError`
- `get_pending_seeds`：分页 `results` → `seeds` 映射
- `save_page_snapshot`：新增 `task_id/task_type/user_prompt` 参数；`html` → `raw_html`
- `start_crawl_task`：新增 `task_type/user_prompt/ai_model/ai_api_url` 参数
- 新增 `check_health()` 方法：`GET /api/health/`

### sandbox/standalone_crawler/handlers.py
- `process_page`：签名新增 `task_type/user_prompt`；`save_page_snapshot` 调用传递新字段

### sandbox/standalone_crawler/crawler.py
- `crawl()` / `_run_bfs_crawl()`：签名新增 `user_prompt`
- `_run_bfs_crawl`：`full` → `formal` 映射变量 `_api_task_type`
- Worker 启动时调用 `check_health()`
- 全部 `start_crawl_task` 和 `_run_bfs_crawl` 调用点传递新字段

---

## 测试验证

- api_client smoke test：8 个方法通过
- handlers smoke test：通过，新字段传递正确
- 所有签名验证通过：`user_prompt`/`task_type` 贯穿 `crawl → _run_bfs_crawl → process_page → save_page_snapshot` 完整链路
