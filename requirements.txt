# ============================================
# 基础 / 通用
# ============================================
python-dotenv==1.0.0
pytest>=8.4
pytest-asyncio>=1.0
black==24.4.2

# ============================================
# Django 全家桶 (成员 C 主导，全员共用)
# ============================================
Django==5.2.14
djangorestframework==3.17.1
psycopg2-binary==2.9.12
django-celery-results==2.5.1
django-cors-headers==4.9.0

# ============================================
# 异步与爬虫引擎 (成员 A)
# ============================================
aiohttp>=3.11.11,<4.0
fake-useragent>=2.0.3
playwright>=1.49.0
scrapy==2.11.2
pybloom-live==4.0.0
redis==5.0.4
celery==5.3.6
flower==2.0.1
minio==7.2.7
requests==2.32.3
beautifulsoup4==4.12.3
lxml~=5.3

# ============================================
# 工具 / 辅助
# ============================================
crawl4ai==0.8.6

# ============================================
# 依赖版本锁定 —— 消除 requests 警告
# ============================================
urllib3>=1.26,<3            # requests 2.32.3 官方要求 <3
charset-normalizer>=2,<4    # 保留，requests 会优先使用它
chardet>=3.0.2,<6           # 强制锁定兼容版本，防止安装 7.x